services:
  - type: web
    name: vc-fantasy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
